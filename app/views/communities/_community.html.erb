<div class="panel panel-default col-md-4">
  <div class="panel-body">
    <td><h2><%= link_to community.name, community.link %></h2></td>
    <td><h5><%= markdown community.categories.map { |t| link_to t, category_path(t) }.join(', ') %></h5></td>
    <td><%= community.about %></td><br><br>
    <td><%= markdown community.link %></td>
    <td><%= markdown community.tags.map { |t| link_to t, tag_path(t), class: "label label-primary" }.join(' ') %></td>
    <td>  
    <div class="col-xs-4">
        <%= link_to "♡", like_community_path(community), method: :put %> <%= community.score %>
      </div>
      <div class="col-xs-4">
        <%= link_to 'More', community %>
      </div>
      <div class="col-xs-4">
        <!-- -->
      </div>
    </div>
  </div>